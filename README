meridian oss
============

build:  make
run:    make run          (rebuilds meridian.db from data/)

binaries land in bin/

  mediation      nightly ingest, writes meridian.db
  inventory-api  :8081   /sites  /circuits  /capacity
  billing-run    monthly invoice register
  invoice-api    :8082   /invoices?acct=...&period=...
  ipam           management address report (10.20.0.0/16)

dashboard/index.html and dashboard/billing.html are static pages, open them in
a browser with inventory-api and invoice-api running.

dashboard/capacity.html is the sales desk capacity check. it asks
/capacity?requested=<mbps> and shows what is left at each customer location.
available = total - allocated - maintenance buffer. utilization counts the
buffer as used. serviceable locations come from data/locations.csv, whose
BUFFER_MBPS column carries the buffer; every row ships at 0 today, so quoted
availability is unchanged until real buffer values are loaded.

capacity arithmetic is not written here. it is vendored in
vendor/oss-capacity/ from the canonical rules repo

  https://github.com/joannayang371/unified-inventory-rules

which vantage-net consumes through the oss-capacity python package. change the
rule there, not here (see vendor/oss-capacity/README).

data/ holds the telemetry drops. billing/accounts/ is the account master.

contacts: ops-eng (pager), billing questions -> finance
